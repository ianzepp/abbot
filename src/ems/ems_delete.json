{
  "name": "ems_delete",
  "description": "Delete rows by their IDs. Returns the number of deleted rows.",
  "parameters": {
    "type": "object",
    "properties": {
      "table": {
        "type": "string",
        "description": "Table name"
      },
      "ids": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Row IDs to delete"
      }
    },
    "required": ["table", "ids"],
    "additionalProperties": false
  }
}
