{
  "name": "head__conclave_request",
  "description": "Request an immediate conclave for strategic guidance. Use when facing decisions that need Mind-level deliberation.",
  "parameters": {
    "type": "object",
    "properties": {
      "reason": { "type": "string", "description": "Why you need strategic guidance" }
    },
    "required": ["reason"],
    "additionalProperties": false
  }
}
