{
  "name": "head__config_read",
  "description": "Read workspace config. Returns entire config, a section, or a specific key.",
  "parameters": {
    "type": "object",
    "properties": {
      "section": {
        "type": "string",
        "description": "Config section (e.g., 'head', 'hand', 'mind', 'tars', 'harness')"
      },
      "key": {
        "type": "string",
        "description": "Key within section"
      }
    },
    "additionalProperties": false
  }
}
