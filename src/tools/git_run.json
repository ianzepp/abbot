{
  "name": "git_run",
  "description": "Run a git command (heads only).",
  "parameters": {
    "type": "object",
    "properties": {
      "args": {
        "type": "string",
        "description": "Arguments to pass to git (e.g. \"status\", \"add src/*.rs\", \"commit -m 'msg'\")"
      }
    },
    "required": ["args"],
    "additionalProperties": false
  }
}
