{
  "name": "hand__fs_list",
  "description": "List files under a directory (workspace-relative paths).",
  "parameters": {
    "type": "object",
    "properties": {
      "path": { "type": "string" },
      "pattern": { "type": "string" },
      "recursive": { "type": "boolean" },
      "max_results": { "type": "integer", "minimum": 1, "maximum": 5000 }
    },
    "additionalProperties": false
  }
}
