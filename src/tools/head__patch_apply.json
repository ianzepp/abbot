{
  "name": "head__patch_apply",
  "description": "Apply a unified diff patch directly (heads only).",
  "parameters": {
    "type": "object",
    "properties": {
      "patch": { "type": "string", "description": "Unified diff patch to apply" }
    },
    "required": ["patch"],
    "additionalProperties": false
  }
}
