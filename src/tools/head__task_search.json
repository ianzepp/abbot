{
  "name": "head__task_search",
  "description": "Search task goals and results.",
  "parameters": {
    "type": "object",
    "properties": {
      "pattern": { "type": "string" },
      "scope": { "type": "string" },
      "limit": { "type": "integer", "minimum": 1, "maximum": 50 }
    },
    "required": ["pattern"],
    "additionalProperties": false
  }
}
