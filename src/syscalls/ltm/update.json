{
  "name": "tool__ltm_update",
  "description": "Update the long-term memory (LTM).",
  "parameters": {
    "type": "object",
    "properties": {
      "ops": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "kind": { "type": "string", "enum": ["append", "replace", "remove"] },
            "content": { "type": "string" },
            "pattern": { "type": "string" }
          },
          "required": ["kind"],
          "additionalProperties": false
        }
      }
    },
    "required": ["ops"],
    "additionalProperties": false
  }
}
