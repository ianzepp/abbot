{
  "name": "tool__docs_read",
  "description": "Read a documentation topic by name.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Document name (from docs:list)"
      }
    },
    "required": ["name"],
    "additionalProperties": false
  }
}
