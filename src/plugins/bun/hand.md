Use `bun` for JS/TS project operations.

- Prefer `bun install` when using bun as the package manager.
- Use `bun run <script>` for package scripts.
- If output is large, rerun with a narrower command or flags that reduce noise.
