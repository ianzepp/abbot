id = "bun"
tool_name = "bun"
description = "Run bun in the sandbox workspace (no shell)."
program = "bun"

timeout_secs = 900

[hand]
expose = true
exec = true
max_stdout_chars = 50000
max_stderr_chars = 10000

[head]
expose = false
exec = false
