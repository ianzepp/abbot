Use `git` for repository operations.

- Run `git status` first and include it in reasoning.
- Prefer `git add <paths>` over `git add -A` unless explicitly requested.
- Before committing, inspect diffs; avoid committing secrets and unrelated files.
- Follow existing commit message style.
