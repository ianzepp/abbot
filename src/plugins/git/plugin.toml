id = "git"
tool_name = "git"
description = "Run git in the sandbox workspace (no shell)."
program = "git"

timeout_secs = 120

[hand]
expose = true
exec = true
max_stdout_chars = 50000
max_stderr_chars = 10000

[head]
expose = false
exec = false
