Use `--output json` for structured data.

Common commands:
- `himalaya list` - list messages
- `himalaya read <id>` - read message
- `himalaya reply <id>` - reply (body via stdin)
- `himalaya send` - send new message
- `himalaya search "query"` - search messages
