{
  "name": "tool__state_query",
  "description": "Query system state: wants, logs, stats.",
  "parameters": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["wants", "logs", "stats"]
      },
      "scope": { "type": "string" },
      "task_id": { "type": "string" },
      "limit": { "type": "integer", "minimum": 1, "maximum": 100 }
    },
    "required": ["mode"],
    "additionalProperties": false
  }
}
