{
  "name": "tool__need_create",
  "description": "Create an immediate need for a head to address. Use when work must happen now. Returns the created need record.",
  "parameters": {
    "type": "object",
    "properties": {
      "need": {
        "type": "string",
        "description": "What needs to happen - the strategic directive"
      },
      "context": {
        "type": "string",
        "description": "Supporting information or reasoning"
      },
      "priority": {
        "type": "string",
        "enum": ["low", "normal", "high", "urgent"],
        "description": "Priority level (default: normal)"
      }
    },
    "required": ["need"],
    "additionalProperties": false
  }
}
