{
  "name": "tool__want_list",
  "description": "List aspirational items from the wants pool. Use to review what could be promoted to immediate needs. Returns an array of want records.",
  "parameters": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "integer",
        "description": "Maximum items to return (default: 20)"
      }
    },
    "additionalProperties": false
  }
}
