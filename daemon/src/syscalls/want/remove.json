{
  "name": "tool__want_remove",
  "description": "Remove an item from the wants pool. Use when a want is completed, obsolete, or duplicated. Returns removal status.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The want ID to remove"
      }
    },
    "required": ["id"],
    "additionalProperties": false
  }
}
