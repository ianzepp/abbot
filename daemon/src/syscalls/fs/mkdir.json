{
  "name": "tool__fs_mkdir",
  "description": "Create a directory, including parent directories by default. Use when a directory must exist before writing files. Returns created status.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": { "type": "string", "description": "Workspace-relative path" },
      "parents": { "type": "boolean", "description": "Create parent directories (default: true)" }
    },
    "required": ["path"],
    "additionalProperties": false
  }
}
