{
  "name": "tool__patch_apply",
  "description": "Apply a unified diff patch to one or more files. Use when you have a precise multi-line edit expressed as a diff. Returns patch application status.",
  "parameters": {
    "type": "object",
    "properties": {
      "patch": { "type": "string", "description": "Unified diff patch to apply" }
    },
    "required": ["patch"],
    "additionalProperties": false
  }
}
