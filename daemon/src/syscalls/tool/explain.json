{
  "name": "tool__tool_explain",
  "description": "Retrieve the full schema and description of an external tool. Use when you encounter an unfamiliar tool name. Returns the tool's parameter schema.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": { "type": "string" },
      "room": { "type": "string", "description": "Optional room override; defaults to the current room" },
      "source": { "type": "string", "enum": ["external"], "description": "Tool source (currently only external)" }
    },
    "required": ["name"],
    "additionalProperties": false
  }
}
