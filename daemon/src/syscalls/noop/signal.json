{
  "name": "tool__noop_signal",
  "description": "Signal that nothing needs attention right now. Call this when you have reviewed the current state and determined no action is warranted. Provide an optional reason.",
  "parameters": {
    "type": "object",
    "properties": {
      "reason": { "type": "string", "description": "Why nothing needs attention right now" }
    },
    "required": [],
    "additionalProperties": false
  }
}
