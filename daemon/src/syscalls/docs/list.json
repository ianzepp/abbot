{
  "name": "tool__docs_list",
  "description": "List all available documentation entries. Returns syscalls/<namespace> docs (auto-generated from tool specs) and skills/<name> docs (user-provided guides). Use before docs_read to discover what's available.",
  "parameters": {
    "type": "object",
    "properties": {},
    "additionalProperties": false
  }
}
