{
  "name": "tool__docs_search",
  "description": "Search documentation for a keyword or phrase. Use when you need to find relevant docs without knowing the exact topic name. Returns matching excerpts.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search term (case-insensitive)"
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 50,
        "description": "Max results (default 20)"
      }
    },
    "required": ["query"],
    "additionalProperties": false
  }
}
