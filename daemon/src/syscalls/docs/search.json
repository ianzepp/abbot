{
  "name": "tool__docs_search",
  "description": "Search all documentation (syscalls and skills) for a keyword or phrase. Returns matching lines with document name and line number.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search term (case-insensitive)"
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 50,
        "description": "Max results (default 20)"
      }
    },
    "required": ["query"],
    "additionalProperties": false
  }
}
