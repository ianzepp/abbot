{
  "name": "tool__docs_read",
  "description": "Read a documentation entry by name. Names use the format syscalls/<namespace> or skills/<name>. Use docs_list to discover available entries.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Document name, e.g. 'syscalls/fs' or 'skills/kubernetes'"
      }
    },
    "required": ["name"],
    "additionalProperties": false
  }
}
