{
  "name": "tool__hand_run",
  "description": "Execute a focused work task using a hand agent's LLM+tool loop. Returns a summary of the work performed. Use this for parallelizable subtasks that need tool access (file reads, searches, code execution).",
  "parameters": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "The task prompt describing what work to perform"
      },
      "context": {
        "type": "string",
        "description": "Optional extra context or input data for the task"
      },
      "max_iters": {
        "type": "integer",
        "description": "Maximum LLM+tool iterations (default: 24)"
      }
    },
    "required": ["prompt"],
    "additionalProperties": false
  }
}
