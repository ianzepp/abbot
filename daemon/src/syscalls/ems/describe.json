{
  "name": "tool__ems_describe",
  "description": "Describe table schemas. Use when you need to discover available columns and types. Returns schema information.",
  "parameters": {
    "type": "object",
    "properties": {
      "table": {
        "type": "string",
        "description": "Table name to describe (omit to list all tables)"
      }
    },
    "additionalProperties": false
  }
}
