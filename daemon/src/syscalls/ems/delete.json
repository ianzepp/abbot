{
  "name": "tool__ems_delete",
  "description": "Delete rows by their IDs. Use when removing entities permanently. Returns the number of deleted rows.",
  "parameters": {
    "type": "object",
    "properties": {
      "table": {
        "type": "string",
        "description": "Table name (e.g. tasks, needs, wants, memories)"
      },
      "ids": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Row IDs to delete"
      }
    },
    "required": ["table", "ids"],
    "additionalProperties": false
  }
}
