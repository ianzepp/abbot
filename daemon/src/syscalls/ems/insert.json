{
  "name": "tool__ems_insert",
  "description": "Insert a new row into a table. Use when creating a new entity (task, memory, etc). Returns the inserted row with generated ID.",
  "parameters": {
    "type": "object",
    "properties": {
      "table": {
        "type": "string",
        "description": "Table name (e.g. tasks, needs, wants, memories)"
      },
      "values": {
        "type": "object",
        "properties": {},
        "additionalProperties": true,
        "description": "Column-value pairs to insert. 'id' is auto-generated if not provided."
      }
    },
    "required": ["table", "values"],
    "additionalProperties": false
  }
}
