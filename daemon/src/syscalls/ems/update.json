{
  "name": "tool__ems_update",
  "description": "Update rows matching a where clause. Use when modifying existing entities. Returns the number of affected rows.",
  "parameters": {
    "type": "object",
    "properties": {
      "table": {
        "type": "string",
        "description": "Table name (e.g. tasks, needs, wants, memories)"
      },
      "where": {
        "type": "object",
        "properties": {},
        "additionalProperties": true,
        "description": "Filter conditions (required). Use bare values for equality or $gt/$lt/etc."
      },
      "changes": {
        "type": "object",
        "properties": {},
        "additionalProperties": true,
        "description": "Column-value pairs to update"
      }
    },
    "required": ["table", "where", "changes"],
    "additionalProperties": false
  }
}
