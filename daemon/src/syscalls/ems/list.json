{
  "name": "tool__ems_list",
  "description": "List rows from a table ordered by most recent first. Use for a quick overview without filters. Returns an array of row objects.",
  "parameters": {
    "type": "object",
    "properties": {
      "table": {
        "type": "string",
        "description": "Table name (e.g. tasks, needs, wants, memories)"
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "description": "Maximum rows to return (default: 100)"
      },
      "offset": {
        "type": "integer",
        "minimum": 0,
        "description": "Rows to skip (for pagination)"
      }
    },
    "required": ["table"],
    "additionalProperties": false
  }
}
