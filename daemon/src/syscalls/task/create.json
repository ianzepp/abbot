{
  "name": "tool__task_create",
  "description": "Queue a task for a hand to execute. Optionally include pre-fetched tool calls via 'calls' array; the harness will execute them before the LLM synthesis step.",
  "parameters": {
    "type": "object",
    "properties": {
      "prompt": { "type": "string", "description": "Task instruction for the hand (or synthesis prompt when calls are provided)." },
      "input": { "type": "string", "description": "Additional input context." },
      "notify_scope": { "type": "string" },
      "calls": {
        "type": "array",
        "description": "Optional batch of syscalls to pre-execute before the LLM synthesis step.",
        "items": {
          "type": "object",
          "properties": {
            "name": { "type": "string", "description": "Syscall name (e.g. fs:read, fs:search)." },
            "args": { "type": "object", "properties": {}, "additionalProperties": true, "description": "Syscall arguments." }
          },
          "required": ["name"]
        }
      }
    },
    "required": ["prompt"],
    "additionalProperties": false
  }
}
