{
  "name": "tool__room_request",
  "description": "Request a room for strategic guidance. Use when facing decisions that need deliberation.",
  "parameters": {
    "type": "object",
    "properties": {
      "reason": { "type": "string", "description": "Why you need strategic guidance" }
    },
    "required": ["reason"],
    "additionalProperties": false
  }
}
