{
  "name": "tool__room_context",
  "description": "Retrieve conversation history from a scope. Use when you need cross-room or cross-task context awareness. Returns chat messages.",
  "parameters": {
    "type": "object",
    "properties": {
      "room": {
        "type": "string",
        "description": "The room to retrieve context from (e.g. 'issue-42', 'main')"
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 200,
        "description": "Maximum messages to return (default: 100)"
      }
    },
    "required": ["room"],
    "additionalProperties": false
  }
}
