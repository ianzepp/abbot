[workspace]
resolver = "3"
members = ["daemon", "monitor", "tui", "cli"]

[workspace.package]
version = "0.2.0"

[profile.dev]
incremental = false
debug = "line-tables-only"

[profile.release]
strip = true
lto = "thin"
